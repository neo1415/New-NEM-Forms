import { Schema } from "../types/schema";

export const submitForm = async (data: Schema) => {
  // TODO: Replace with actual API endpoint
  await new Promise((resolve) => setTimeout(resolve, 1000));
  return data;
}; 